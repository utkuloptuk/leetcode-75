using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace leetcode_75._141_linked_list_cycle;

public class SolutionTest
{
    [TestMethod]
    // [DataRow(new LinkedList<int> {4,2,0,-4}  , 1,true)]
    // [DataRow(new int[] { 7, 6, 4, 3, 1 }, 0)]
    public void best_time_best_sell_max_profit_is_correct(int[] nodeList, int pos, bool expected)
    {
        // Testi nasıl yazılır arastırılmalı
        // ListNode current = Head;
        // while (current != null)
        // {
        //     Console.Write(current.Value + " ");
        //     current = current.Next;
        // }
        // var result = _121_best_time_to_buy_and_sell_stock.Solution.BestTimetoBuySellStock(transactionList);
        // Assert.AreEqual(maxProfit, result);
    }
}